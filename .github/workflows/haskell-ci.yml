# This GitHub workflow config has been generated by a script via
#
#   haskell-ci 'github' '--config=cabal.haskell-ci' 'cabal.project'
#
# To regenerate the script (for example after adjusting tested-with) run
#
#   haskell-ci regenerate
#
# For more information, see https://github.com/haskell-CI/haskell-ci
#
# version: 0.15.20220525
#
# REGENDATA ("0.15.20220525",["github","--config=cabal.haskell-ci","cabal.project"])
#
name: Haskell-CI
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  linux:
    name: Haskell-CI - Linux - ${{ matrix.compiler }}
    runs-on: ubuntu-20.04
    timeout-minutes:
      60
    container:
      image: buildpack-deps:bionic
    continue-on-error: ${{ matrix.allow-failure }}
    strategy:
      matrix:
        include:
          - compiler: ghc-8.10.7
            compilerKind: ghc
            compilerVersion: 8.10.7
            setup-method: ghcup
            allow-failure: false
      fail-fast: false
    steps:
      - name: apt
        run: |
          apt-get update
          apt-get install -y --no-install-recommends gnupg ca-certificates dirmngr curl git software-properties-common libtinfo5
          mkdir -p "$HOME/.ghcup/bin"
          curl -sL https://downloads.haskell.org/ghcup/0.1.17.8/x86_64-linux-ghcup-0.1.17.8 > "$HOME/.ghcup/bin/ghcup"
          chmod a+x "$HOME/.ghcup/bin/ghcup"
          "$HOME/.ghcup/bin/ghcup" install ghc "$HCVER"
          "$HOME/.ghcup/bin/ghcup" install cabal 3.6.2.0
        env:
          HCKIND: ${{ matrix.compilerKind }}
          HCNAME: ${{ matrix.compiler }}
          HCVER: ${{ matrix.compilerVersion }}
      - name: Set PATH and environment variables
        run: |
          echo "$HOME/.cabal/bin" >> $GITHUB_PATH
          echo "LANG=C.UTF-8" >> "$GITHUB_ENV"
          echo "CABAL_DIR=$HOME/.cabal" >> "$GITHUB_ENV"
          echo "CABAL_CONFIG=$HOME/.cabal/config" >> "$GITHUB_ENV"
          HCDIR=/opt/$HCKIND/$HCVER
          HC=$HOME/.ghcup/bin/$HCKIND-$HCVER
          echo "HC=$HC" >> "$GITHUB_ENV"
          echo "HCPKG=$HOME/.ghcup/bin/$HCKIND-pkg-$HCVER" >> "$GITHUB_ENV"
          echo "HADDOCK=$HOME/.ghcup/bin/haddock-$HCVER" >> "$GITHUB_ENV"
          echo "CABAL=$HOME/.ghcup/bin/cabal-3.6.2.0 -vnormal+nowrap" >> "$GITHUB_ENV"
          HCNUMVER=$(${HC} --numeric-version|perl -ne '/^(\d+)\.(\d+)\.(\d+)(\.(\d+))?$/; print(10000 * $1 + 100 * $2 + ($3 == 0 ? $5 != 1 : $3))')
          echo "HCNUMVER=$HCNUMVER" >> "$GITHUB_ENV"
          echo "ARG_TESTS=--enable-tests" >> "$GITHUB_ENV"
          echo "ARG_BENCH=--enable-benchmarks" >> "$GITHUB_ENV"
          echo "HEADHACKAGE=false" >> "$GITHUB_ENV"
          echo "ARG_COMPILER=--$HCKIND --with-compiler=$HC" >> "$GITHUB_ENV"
          echo "GHCJSARITH=0" >> "$GITHUB_ENV"
        env:
          HCKIND: ${{ matrix.compilerKind }}
          HCNAME: ${{ matrix.compiler }}
          HCVER: ${{ matrix.compilerVersion }}
      - name: env
        run: |
          env
      - name: write cabal config
        run: |
          mkdir -p $CABAL_DIR
          cat >> $CABAL_CONFIG <<EOF
          remote-build-reporting: anonymous
          write-ghc-environment-files: never
          remote-repo-cache: $CABAL_DIR/packages
          logs-dir:          $CABAL_DIR/logs
          world-file:        $CABAL_DIR/world
          extra-prog-path:   $CABAL_DIR/bin
          symlink-bindir:    $CABAL_DIR/bin
          installdir:        $CABAL_DIR/bin
          build-summary:     $CABAL_DIR/logs/build.log
          store-dir:         $CABAL_DIR/store
          install-dirs user
            prefix: $CABAL_DIR
          repository hackage.haskell.org
            url: http://hackage.haskell.org/
          EOF
          cat >> $CABAL_CONFIG <<EOF
          program-default-options
            ghc-options: $GHCJOBS +RTS -M3G -RTS
          EOF
          cat $CABAL_CONFIG
      - name: versions
        run: |
          $HC --version || true
          $HC --print-project-git-commit-id || true
          $CABAL --version || true
      - name: update cabal index
        run: |
          $CABAL v2-update -v
      - name: install cabal-plan
        run: |
          mkdir -p $HOME/.cabal/bin
          curl -sL https://github.com/haskell-hvr/cabal-plan/releases/download/v0.6.2.0/cabal-plan-0.6.2.0-x86_64-linux.xz > cabal-plan.xz
          echo 'de73600b1836d3f55e32d80385acc055fd97f60eaa0ab68a755302685f5d81bc  cabal-plan.xz' | sha256sum -c -
          xz -d < cabal-plan.xz > $HOME/.cabal/bin/cabal-plan
          rm -f cabal-plan.xz
          chmod a+x $HOME/.cabal/bin/cabal-plan
          cabal-plan --version
      - name: install cabal-docspec
        run: |
          mkdir -p $HOME/.cabal/bin
          curl -sL https://github.com/phadej/cabal-extras/releases/download/cabal-docspec-0.0.0.20211114/cabal-docspec-0.0.0.20211114.xz > cabal-docspec.xz
          echo 'e224700d9e8c9ec7ec6bc3f542ba433cd9925a5d356676c62a9bd1f2c8be8f8a  cabal-docspec.xz' | sha256sum -c -
          xz -d < cabal-docspec.xz > $HOME/.cabal/bin/cabal-docspec
          rm -f cabal-docspec.xz
          chmod a+x $HOME/.cabal/bin/cabal-docspec
          cabal-docspec --version
      - name: checkout
        uses: actions/checkout@v2
        with:
          path: source
      - name: initial cabal.project for sdist
        run: |
          touch cabal.project
          echo "packages: $GITHUB_WORKSPACE/source/cabal-bundler" >> cabal.project
          echo "packages: $GITHUB_WORKSPACE/source/cabal-deps" >> cabal.project
          echo "packages: $GITHUB_WORKSPACE/source/cabal-diff" >> cabal.project
          echo "packages: $GITHUB_WORKSPACE/source/cabal-docspec" >> cabal.project
          echo "packages: $GITHUB_WORKSPACE/source/cabal-env" >> cabal.project
          echo "packages: $GITHUB_WORKSPACE/source/cabal-haddock-server" >> cabal.project
          echo "packages: $GITHUB_WORKSPACE/source/cabal-hasklint" >> cabal.project
          echo "packages: $GITHUB_WORKSPACE/source/cabal-hie" >> cabal.project
          echo "packages: $GITHUB_WORKSPACE/source/cabal-iface-query" >> cabal.project
          echo "packages: $GITHUB_WORKSPACE/source/cabal-store-check" >> cabal.project
          echo "packages: $GITHUB_WORKSPACE/source/cabal-store-gc" >> cabal.project
          echo "packages: $GITHUB_WORKSPACE/source/peura" >> cabal.project
          cat cabal.project
      - name: sdist
        run: |
          mkdir -p sdist
          $CABAL sdist all --output-dir $GITHUB_WORKSPACE/sdist
      - name: unpack
        run: |
          mkdir -p unpacked
          find sdist -maxdepth 1 -type f -name '*.tar.gz' -exec tar -C $GITHUB_WORKSPACE/unpacked -xzvf {} \;
      - name: generate cabal.project
        run: |
          PKGDIR_cabal_bundler="$(find "$GITHUB_WORKSPACE/unpacked" -maxdepth 1 -type d -regex '.*/cabal-bundler-[0-9.]*')"
          echo "PKGDIR_cabal_bundler=${PKGDIR_cabal_bundler}" >> "$GITHUB_ENV"
          PKGDIR_cabal_deps="$(find "$GITHUB_WORKSPACE/unpacked" -maxdepth 1 -type d -regex '.*/cabal-deps-[0-9.]*')"
          echo "PKGDIR_cabal_deps=${PKGDIR_cabal_deps}" >> "$GITHUB_ENV"
          PKGDIR_cabal_diff="$(find "$GITHUB_WORKSPACE/unpacked" -maxdepth 1 -type d -regex '.*/cabal-diff-[0-9.]*')"
          echo "PKGDIR_cabal_diff=${PKGDIR_cabal_diff}" >> "$GITHUB_ENV"
          PKGDIR_cabal_docspec="$(find "$GITHUB_WORKSPACE/unpacked" -maxdepth 1 -type d -regex '.*/cabal-docspec-[0-9.]*')"
          echo "PKGDIR_cabal_docspec=${PKGDIR_cabal_docspec}" >> "$GITHUB_ENV"
          PKGDIR_cabal_env="$(find "$GITHUB_WORKSPACE/unpacked" -maxdepth 1 -type d -regex '.*/cabal-env-[0-9.]*')"
          echo "PKGDIR_cabal_env=${PKGDIR_cabal_env}" >> "$GITHUB_ENV"
          PKGDIR_cabal_haddock_server="$(find "$GITHUB_WORKSPACE/unpacked" -maxdepth 1 -type d -regex '.*/cabal-haddock-server-[0-9.]*')"
          echo "PKGDIR_cabal_haddock_server=${PKGDIR_cabal_haddock_server}" >> "$GITHUB_ENV"
          PKGDIR_cabal_hasklint="$(find "$GITHUB_WORKSPACE/unpacked" -maxdepth 1 -type d -regex '.*/cabal-hasklint-[0-9.]*')"
          echo "PKGDIR_cabal_hasklint=${PKGDIR_cabal_hasklint}" >> "$GITHUB_ENV"
          PKGDIR_cabal_hie="$(find "$GITHUB_WORKSPACE/unpacked" -maxdepth 1 -type d -regex '.*/cabal-hie-[0-9.]*')"
          echo "PKGDIR_cabal_hie=${PKGDIR_cabal_hie}" >> "$GITHUB_ENV"
          PKGDIR_cabal_iface_query="$(find "$GITHUB_WORKSPACE/unpacked" -maxdepth 1 -type d -regex '.*/cabal-iface-query-[0-9.]*')"
          echo "PKGDIR_cabal_iface_query=${PKGDIR_cabal_iface_query}" >> "$GITHUB_ENV"
          PKGDIR_cabal_store_check="$(find "$GITHUB_WORKSPACE/unpacked" -maxdepth 1 -type d -regex '.*/cabal-store-check-[0-9.]*')"
          echo "PKGDIR_cabal_store_check=${PKGDIR_cabal_store_check}" >> "$GITHUB_ENV"
          PKGDIR_cabal_store_gc="$(find "$GITHUB_WORKSPACE/unpacked" -maxdepth 1 -type d -regex '.*/cabal-store-gc-[0-9.]*')"
          echo "PKGDIR_cabal_store_gc=${PKGDIR_cabal_store_gc}" >> "$GITHUB_ENV"
          PKGDIR_peura="$(find "$GITHUB_WORKSPACE/unpacked" -maxdepth 1 -type d -regex '.*/peura-[0-9.]*')"
          echo "PKGDIR_peura=${PKGDIR_peura}" >> "$GITHUB_ENV"
          rm -f cabal.project cabal.project.local
          touch cabal.project
          touch cabal.project.local
          echo "packages: ${PKGDIR_cabal_bundler}" >> cabal.project
          echo "packages: ${PKGDIR_cabal_deps}" >> cabal.project
          echo "packages: ${PKGDIR_cabal_diff}" >> cabal.project
          echo "packages: ${PKGDIR_cabal_docspec}" >> cabal.project
          echo "packages: ${PKGDIR_cabal_env}" >> cabal.project
          echo "packages: ${PKGDIR_cabal_haddock_server}" >> cabal.project
          echo "packages: ${PKGDIR_cabal_hasklint}" >> cabal.project
          echo "packages: ${PKGDIR_cabal_hie}" >> cabal.project
          echo "packages: ${PKGDIR_cabal_iface_query}" >> cabal.project
          echo "packages: ${PKGDIR_cabal_store_check}" >> cabal.project
          echo "packages: ${PKGDIR_cabal_store_gc}" >> cabal.project
          echo "packages: ${PKGDIR_peura}" >> cabal.project
          echo "package cabal-bundler" >> cabal.project
          echo "    ghc-options: -Werror=missing-methods" >> cabal.project
          echo "package cabal-deps" >> cabal.project
          echo "    ghc-options: -Werror=missing-methods" >> cabal.project
          echo "package cabal-diff" >> cabal.project
          echo "    ghc-options: -Werror=missing-methods" >> cabal.project
          echo "package cabal-docspec" >> cabal.project
          echo "    ghc-options: -Werror=missing-methods" >> cabal.project
          echo "package cabal-env" >> cabal.project
          echo "    ghc-options: -Werror=missing-methods" >> cabal.project
          echo "package cabal-haddock-server" >> cabal.project
          echo "    ghc-options: -Werror=missing-methods" >> cabal.project
          echo "package cabal-hasklint" >> cabal.project
          echo "    ghc-options: -Werror=missing-methods" >> cabal.project
          echo "package cabal-hie" >> cabal.project
          echo "    ghc-options: -Werror=missing-methods" >> cabal.project
          echo "package cabal-iface-query" >> cabal.project
          echo "    ghc-options: -Werror=missing-methods" >> cabal.project
          echo "package cabal-store-check" >> cabal.project
          echo "    ghc-options: -Werror=missing-methods" >> cabal.project
          echo "package cabal-store-gc" >> cabal.project
          echo "    ghc-options: -Werror=missing-methods" >> cabal.project
          echo "package peura" >> cabal.project
          echo "    ghc-options: -Werror=missing-methods" >> cabal.project
          cat >> cabal.project <<EOF
          packages:    http://oleg.fi/gentle-introduction-2022.5.tar.gz
          packages:
            https://github.com/phadej/warp-wo-x509/releases/download/v3.3.17/warp-3.3.17.tar.gz

          packages:
            https://github.com/phadej/hooglite/releases/download/v0.20211229/hooglite-0.20211229.tar.gz

          constraints: Cabal ^>=3.6
          constraints: optics-core ^>=0.4.1
          constraints: directory     installed
          constraints: process       installed
          constraints: transformers  installed
          constraints: ghc-lib-parser-ex -no-ghc-lib -auto
          constraints: cryptonite <0
          EOF
          $HCPKG list --simple-output --names-only | perl -ne 'for (split /\s+/) { print "constraints: $_ installed\n" unless /^(Cabal|cabal-bundler|cabal-deps|cabal-diff|cabal-docspec|cabal-env|cabal-haddock-server|cabal-hasklint|cabal-hie|cabal-iface-query|cabal-store-check|cabal-store-gc|peura)$/; }' >> cabal.project.local
          cat cabal.project
          cat cabal.project.local
      - name: dump install plan
        run: |
          $CABAL v2-build $ARG_COMPILER $ARG_TESTS $ARG_BENCH --dry-run all
          cabal-plan
      - name: cache
        uses: actions/cache@v2
        with:
          key: ${{ runner.os }}-${{ matrix.compiler }}-${{ github.sha }}
          path: ~/.cabal/store
          restore-keys: ${{ runner.os }}-${{ matrix.compiler }}-
      - name: install dependencies
        run: |
          $CABAL v2-build $ARG_COMPILER --disable-tests --disable-benchmarks --dependencies-only -j2 all
          $CABAL v2-build $ARG_COMPILER $ARG_TESTS $ARG_BENCH --dependencies-only -j2 all
      - name: build w/o tests
        run: |
          $CABAL v2-build $ARG_COMPILER --disable-tests --disable-benchmarks all
      - name: build
        run: |
          $CABAL v2-build $ARG_COMPILER $ARG_TESTS $ARG_BENCH all --write-ghc-environment-files=always
      - name: tests
        run: |
          $CABAL v2-test $ARG_COMPILER $ARG_TESTS $ARG_BENCH all --test-show-details=direct
      - name: docspec
        run: |
          $CABAL v2-build $ARG_COMPILER $ARG_TESTS $ARG_BENCH all
          cabal-docspec $ARG_COMPILER --verbose
      - name: cabal check
        run: |
          cd ${PKGDIR_cabal_bundler} || false
          ${CABAL} -vnormal check
          cd ${PKGDIR_cabal_deps} || false
          ${CABAL} -vnormal check
          cd ${PKGDIR_cabal_diff} || false
          ${CABAL} -vnormal check
          cd ${PKGDIR_cabal_docspec} || false
          ${CABAL} -vnormal check
          cd ${PKGDIR_cabal_env} || false
          ${CABAL} -vnormal check
          cd ${PKGDIR_cabal_haddock_server} || false
          ${CABAL} -vnormal check
          cd ${PKGDIR_cabal_hasklint} || false
          ${CABAL} -vnormal check
          cd ${PKGDIR_cabal_hie} || false
          ${CABAL} -vnormal check
          cd ${PKGDIR_cabal_iface_query} || false
          ${CABAL} -vnormal check
          cd ${PKGDIR_cabal_store_check} || false
          ${CABAL} -vnormal check
          cd ${PKGDIR_cabal_store_gc} || false
          ${CABAL} -vnormal check
          cd ${PKGDIR_peura} || false
          ${CABAL} -vnormal check
      - name: haddock
        run: |
          $CABAL v2-haddock --haddock-all $ARG_COMPILER --with-haddock $HADDOCK $ARG_TESTS $ARG_BENCH all
      - name: unconstrained build
        run: |
          rm -f cabal.project.local
          $CABAL v2-build $ARG_COMPILER --disable-tests --disable-benchmarks all
